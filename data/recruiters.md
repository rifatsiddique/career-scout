# Recruiters

Roster of recruiter contacts. One row per recruiter; full history (known facts,
job threads, reply log) lives in `data/recruiters/{slug}.md`. Written by the
`recruiter` mode — read only by `recruiter --list`, never in the main draft flow.

| Recruiter | Agency/Employer | Threads | Open | Last Contact | Dossier |
|-----------|-----------------|---------|------|--------------|---------|
